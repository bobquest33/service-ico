kind: PersistentVolume
apiVersion: v1
metadata:
  name: notification-service-database-volume
  labels:
    release: "production"
  namespace: notification-service
spec:
  capacity:
    storage: 50Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Recycle
  gcePersistentDisk:
    fsType: "ext4"
    pdName: "notification-service-database"
---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: notification-service-data-claim
  namespace: notification-service
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  storageClassName: ""
  selector:
    matchLabels:
      release: "production"
---
apiVersion: v1
kind: Pod
metadata:
  name: database-postgres
  namespace: notification-service
  labels:
    name: database
    node: postgres
spec:
  containers:
    -
      name: postgres
      image: postgres
      ports:
        -
          containerPort: 5432
      env:
        - name: PGDATA
          value: /var/lib/postgresql/data/pgdata
      volumeMounts:
        - mountPath: /var/lib/postgresql/data
          name: pg-data-volume
  volumes:
    - name: pg-data-volume
      persistentVolumeClaim:
        claimName: notification-service-data-claim
---
apiVersion: v1
kind: Service
metadata:
  name: database-postgres-service
  namespace: notification-service
  labels:
    name: database
    node: postgres
spec:
  ports:
    - port: 5432
      targetPort: 5432
  externalIPs:
    - 192.168.99.100
  selector:
    name: database
    node: postgres